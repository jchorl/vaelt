package config

const (
	// SessionSecret is the secret used to sign sessions
	SessionSecret = "sessionSecret"

	// ApplicationID is the id of this application
	// ApplicationID = "https://vaelt.xyz"
	ApplicationID = "https://localhost:3000"
)
